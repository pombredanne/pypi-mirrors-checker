============
Installation
============

At the command line::

    $ easy_install pypimirrorschecker

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pypimirrorschecker
    $ pip install pypimirrorschecker
